<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MoviesCatalog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/total_black</item>
        <item name="android:background">@color/black</item>
    </style>

    <style name="Theme.MoviesCatalog.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.MoviesCatalog</item>
    </style>

    <style name="AppBaseTheme" parent="android:Theme.Material">
        <item name="android:datePickerDialogTheme">@style/MyDatePickerDialogTheme</item>
    </style>

    <style name="MyDatePickerDialogTheme" parent="android:Theme.Material.Dialog">
        <!-- If you want you can override only colorAccent -->
        <item name="android:colorAccent">@color/dark_red</item>
    </style>
</resources>